import Recat from "react";

// Stateful Component
// Previous knowledge of lifecycle methods is needed

function TimeInterval(props) {
  const tick = (props) => {
    // Write the function tick that will update the value of seconds.
  };

  // Use `componentDidMount()` to help set the time interval

  // Use `componentWillUnmount()` to help clear the time interval

  return <div>Seconds: {`Changing Value of Seconds`}</div>;
}

export default TimeInterval;
